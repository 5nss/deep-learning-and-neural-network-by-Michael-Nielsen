Explain why the presence of the xj term in the gradient equation means learning will still be slow when an input xj is near zero, regardless of the cost function used 
    In the first layer of the network, the "activation" is just the input xi so if xj is small or near to 0 the entire gradient become zero regardless of how much error it is 
Data Expansion: Identify a potential problem with expanding MNIST training data by allowing arbitrarily large rotations of training images
 soln - 
 lets consider - large variation result in change of the result class that specific to it like 6 is rotated by 180 degree it will turn into 9 so that problem arises

Obstacles to Gradient Descent: Identify obstacles to using gradient descent to determine the hyper-parameters λ (regularization parameter) and η (learning rate)
  soln - lamda - we cannot apply gd here because the actual concept of usong lamds is to increase the cost function value if we apply gd to lamda then it will get that value to the 0 so the overall function of the regularisation is defeated here 
  eta - here also we cannot apply gd because here eta is not a variable the influnce the cost funstion its just the step size of the next iteration of the cost function

implemented early stoping in the network2.py
  used n = 5,
  the network stopped traning at 18th epoch instead of 30 epoch 
hence succeesfully stoped the over training issue

What would go wrong if we used μ>1
 in the momentum technique? - every time the ball moves, it gains speed regardless of the gradient. The velocity will grow to infinity, and the weights will explode
What would go wrong if we used μ<0
 in the momentum technique? - This would cause the ball to "flip" its velocity every step and act like vibrating between + and - (like =5,-4.5,+4.0,-4.6)


